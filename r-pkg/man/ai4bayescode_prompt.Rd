% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{ai4bayescode_prompt}
\alias{ai4bayescode_prompt}
\title{Assemble the AI4BayesCode codegen prompt (no network)}
\usage{
ai4bayescode_prompt(
  model_description,
  backend = c("R", "Python", "both"),
  output_path = "./generated",
  classname = NULL,
  priors = "noninformative",
  max_attempts = 5L,
  include_skills = FALSE,
  skills = NULL,
  confirm_model = FALSE
)
}
\arguments{
\item{model_description}{Model description (string) or a `.txt`/`.md` path.}

\item{backend}{`"R"` (default), `"Python"`, or `"both"`.}

\item{output_path}{Output folder the generated files target.}

\item{classname}{C++/Rcpp class name; default derived from the description.}

\item{priors}{`"noninformative"` (default), `"weakly"`, `"interactive"`, or a
named list of parameter -> prior.}

\item{max_attempts}{Max generation attempts to instruct (default 5).}

\item{include_skills}{If `TRUE`, inline the backend skill subset into the system
prompt (for a single-shot completion). If `FALSE` (default), `start.md`-anchored.}

\item{skills}{Optional skill file names to inline (overrides the default subset).}

\item{confirm_model}{If `TRUE`, instruct the model to confirm its understanding of
the model (likelihood, parameters, priors) via `ask_user` before writing any code.}
}
\value{
A list with `system`, `user`, `classname`, `backend`, `skills`.
}
\description{
Builds the system + user prompt for natural-language -> sampler code, from the
bundled skill corpus and a set of settings. Pure and offline.
}
