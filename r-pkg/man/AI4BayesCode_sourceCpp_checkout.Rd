% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{AI4BayesCode_sourceCpp_checkout}
\alias{AI4BayesCode_sourceCpp_checkout}
\alias{AI4BayesCode_sourceCpp}
\title{Compile and load an AI4BayesCode sampler from a standalone checkout}
\usage{
AI4BayesCode_sourceCpp(
  cpp_file,
  AI4BayesCode_path = "./AI4BayesCode",
  rebuild = TRUE,
  verbose = FALSE,
  extra_cppflags = character(),
  extra_libs = character()
)
}
\arguments{
\item{cpp_file}{Path to the `.cpp` file declaring the Rcpp module.}

\item{AI4BayesCode_path}{Path to the AI4BayesCode folder containing the
`include/`, `libgp_kernels/`, and `celerite/` subtrees.}

\item{rebuild}{Logical; force recompilation. Defaults to `TRUE`.}

\item{verbose}{Logical; pass compiler output through. Defaults to `FALSE`.}

\item{extra_cppflags}{Character vector of extra `-I`/`-D` flags appended to
`PKG_CPPFLAGS`.}

\item{extra_libs}{Character vector of extra linker flags appended to
`PKG_LIBS`.}
}
\value{
Invisibly `NULL`; the side effect is that the module class becomes
  loadable in the global environment.
}
\description{
Wraps `Rcpp::sourceCpp()` with a generated Makevars file so the
  AI4BayesCode include roots (mcmclib, BaseMatrixOps, eigen, libgp, celerite)
  and required preprocessor defines are forwarded to the compiler. This is the
  `source()`-the-helpers entry point referenced by the generated
  `example_<Class>.R` and the skill templates; it expects a reachable
  AI4BayesCode checkout (use [ai4bayescode_source()] for the self-contained,
  install-only path).
}
