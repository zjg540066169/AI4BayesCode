% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{ai4bayescode_run_chains}
\alias{ai4bayescode_run_chains}
\title{Run multiple MCMC chains for an AI4BayesCode wrapper class}
\usage{
ai4bayescode_run_chains(
  model_ctor,
  n_chains = 4,
  n_burn = 2000,
  n_keep = 10000,
  seeds = NULL,
  parallel = TRUE,
  mc.cores = NULL,
  verbose = TRUE
)
}
\arguments{
\item{model_ctor}{Function of one argument (the integer seed, passed positionally; the parameter name is irrelevant) returning a fresh wrapper
object exposing `step()` and `get_history()`. Must be deterministic in
`seed`.}

\item{n_chains}{Number of chains. Defaults to 4.}

\item{n_burn}{Warmup iterations per chain. Defaults to 2000.}

\item{n_keep}{Kept iterations per chain. Defaults to 10000.}

\item{seeds}{Optional integer vector of length `n_chains`; auto-generated if
`NULL`.}

\item{parallel}{Logical; use `parallel::mclapply` on POSIX. Defaults to
`TRUE` (silently sequential on Windows).}

\item{mc.cores}{Number of cores; auto-chosen if `NULL`.}

\item{verbose}{Logical; print a progress message. Defaults to `TRUE`.}
}
\value{
A list with `histories` (list of per-chain `get_history()` returns),
  `seeds`, and `wall` (per-chain seconds).
}
\description{
Spawns `n_chains` chains (parallel via `parallel::mclapply` on
  POSIX, or sequential) from a deterministic constructor closure, runs warmup
  + keep on each, and returns the per-chain `get_history()` results, the seeds
  used, and the per-chain wall time. Each chain instantiates a fresh Rcpp
  module object in its own worker, so object ownership is handled cleanly.
}
